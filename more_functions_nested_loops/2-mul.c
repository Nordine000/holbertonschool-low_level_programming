#include "main.h"
#include <stdio.h>

/**
 * mul- check the code
 * @a: on lui a assigner une valeur
 * @b: on lui a assigner une valeur
 * Return: Always 0.
 */

int mul(int a, int b)
{
return (a * b);
}
