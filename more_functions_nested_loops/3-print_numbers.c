#include "main.h"
#include <stdio.h>
/**
 * print_numbers- check the code
 *
 * Return: Always 0.
 */
void print_numbers(void)
{
	int a;
for (a = 0; a <= 10; a++)
{
	putchar(a + '0');
}
	putchar('\n');
}
