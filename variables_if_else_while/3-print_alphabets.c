#include <stdio.h>
#include <ctype.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char c;
char u;
for (c = 'A'; c <= 'A'; c++)
{
putchar(c);
putchar(tolower(c));
}
putchar('\n');
return (0);
}
