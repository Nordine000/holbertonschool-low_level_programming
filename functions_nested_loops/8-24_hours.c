#include "main.h"
#include <stdio.h>
/**
 * jack_bauer- check the code
 *
 * Return: Always 0.
 */
void jack_bauer(void)
{
	int h, m;
for (int h = 0; hour < 24; hour++)
{
for (int m = 0; minute < 60; minute++)
{
putchar("%02d : %02d\n", h, m);
}
}
}
